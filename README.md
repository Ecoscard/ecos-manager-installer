# ECOS Manager Installer

Instalador oficial do ECOS Manager.

Este repositório contém apenas o bootstrap de instalação do ECOS Manager.

O código-fonte principal do ECOS Manager não é distribuído por este repositório.

## Instalação

A instalação será realizada por meio do script:

```bash
install.sh
